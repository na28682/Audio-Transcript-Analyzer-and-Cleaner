# Core dependencies for Audio Transcript Analyzer and Cleaner
whisperx>=3.1.0
torch>=2.0.0
torchaudio>=2.0.0
pandas>=1.5.0
numpy>=1.24.0
tqdm>=4.65.0
pydub>=0.25.1

# Optional but recommended for better audio format support
soundfile>=0.12.0
librosa>=0.10.0

# Hugging Face Hub (for speaker diarization models)
huggingface-hub>=0.16.0

